import {PublicStoreHandler} from 'hotballoon'

/**
 * @extends PublicStoreHandler
 */
export class StoreHandler{{store}} extends PublicStoreHandler {
  /**
   *
   * @param {StoreInterface} store
   */
  constructor(store) {
    super(store)
  }
}
