{
  "arguments":
  [
    {
      "argument": "action",
      "defaultValue": "template",
      "format": "^[A-Z][a-zA-Z0-9]*$"
    },
    {
      "argument": "store",
      "defaultValue": "template",
      "format": "^[A-Z][a-zA-Z0-9]*$"
    },
    {
      "argument": "component",
      "defaultValue": "template",
      "format": "^[A-Z][a-zA-Z0-9]*$"
    },
    {
      "argument": "viewcontainer",
      "defaultValue": "template",
      "format": "^[A-Z][a-zA-Z0-9]*$"
    },
    {
      "argument": "view",
      "defaultValue": "template",
      "format": "^[A-Z][a-zA-Z0-9]*$"
    }
  ]
}